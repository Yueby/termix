{
  "private": true,
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "scripts": {
    "dev": "pnpm --filter @termix/client dev",
    "build": "pnpm --filter @termix/client build",
    "tauri": "pnpm --filter @termix/client tauri"
  }
}
